using Godot;
using System;

public partial class SceneComplete : Node
{
    // [Export] Node2D Train1;

    // Vector2 screenSize;

    // public override void _Ready()
    // {
    //     screenSize = GetViewport().GetVisibleRect().Size;
    // }


    // public override void _Input(InputEvent @event)
    // {
    //     GD.Print(screenSize, Train1.GlobalPosition.X);
    //     if (Train1.Position.X >= screenSize.X) GD.Print("OUT");
    // }
}

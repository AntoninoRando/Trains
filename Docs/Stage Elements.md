# Keys

- `StageElement`: An _abstract_ class to be inherited by any stage element.
- `MatchOrchestrator`: A class that puts together different objects (train spawner, stage elements spawner) to build a complete playable match.
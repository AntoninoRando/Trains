{
    "files.exclude": {
        // Ignoring files created and maintained by godot (that should not be 
        // externally handled)
        "**/*.uid": true,
        "**/*.import": true
    },
    "editor.rulers": [80]
}